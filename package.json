{
  "name": "bahasaweb-table-of-contents",
  "version": "1.0.0",
  "description": "Generate otomatis menggunakan bahasa inggris",
  "author": "Aris Munandar",
  "license": "GPL-2.0-or-later",
  "main": "build/index.js",
  "scripts": {
    "build": "wp-scripts build",
    "start": "wp-scripts start",
    "format": "wp-scripts format",
    "lint:css": "wp-scripts lint-style",
    "lint:js": "wp-scripts lint-js",
    "packages-update": "wp-scripts packages-update",
    "package": "npm run build && zip -r bahasaweb-table-of-contents.zip . -x 'node_modules/*' -x '.git/*' -x '.*' -x '*.log' -x 'package-lock.json'"
  },
  "devDependencies": {
    "@wordpress/scripts": "^26.0.0"
  },
  "dependencies": {}
}
